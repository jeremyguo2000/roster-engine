FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 5173

# Source mounted as volume in dev; node_modules kept inside container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
